/** AppliTrack persistence: Supabase Auth, private households and family sharing. */
import { APP_CONFIG } from './config.js';
const cache={appliances:[],services:[],family:[],invites:[],currentUser:null}; let client=null,householdId=null;
const api=()=>{if(client)return client;if(!window.supabase||!APP_CONFIG.supabaseUrl||!APP_CONFIG.supabaseAnonKey)return null;client=window.supabase.createClient(APP_CONFIG.supabaseUrl,APP_CONFIG.supabaseAnonKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});return client;};
const dbToApp=x=>({id:x.id,name:x.name,brand:x.brand,model:x.model,serialNumber:x.serial_number||'',purchaseDate:x.purchase_date,purchasePrice:Number(x.purchase_price),warrantyMonths:Number(x.warranty_months),lifespanYears:Number(x.lifespan_years),room:x.room,status:x.status,imageUrl:x.image_url||'',notes:x.notes||''});
const appToDB=x=>({id:x.id,household_id:householdId,name:x.name,brand:x.brand,model:x.model,serial_number:x.serialNumber||null,purchase_date:x.purchaseDate,purchase_price:x.purchasePrice,warranty_months:x.warrantyMonths,lifespan_years:x.lifespanYears,room:x.room,status:x.status,image_url:x.imageUrl||null,notes:x.notes||null});
const dbToService=x=>({id:x.id,applianceId:x.appliance_id,date:x.date,type:x.type,cost:Number(x.cost),technician:x.technician||'',phone:x.phone||'',description:x.description});
const serviceToDB=x=>({id:x.id,appliance_id:x.applianceId,date:x.date,type:x.type,cost:x.cost,technician:x.technician||null,phone:x.phone||null,description:x.description});
const dbToProfile=x=>({id:x.id,name:x.full_name||x.email,email:x.email,role:x.role||'Viewer',profilePic:x.profile_pic||''});
export const dbConfig={isConnected:()=>Boolean(api()),getSupabaseUrl:()=>APP_CONFIG.supabaseUrl,getSupabaseKey:()=>'',setCredentials:()=>{throw new Error('Configure js/config.js before publishing.');}};
export const db={
 async init(){if(!api())return;const {data:{session}}=await api().auth.getSession();if(session)await this.sync();},isConfigured:()=>Boolean(api()),isAuthenticated:()=>Boolean(cache.currentUser),
 async signUp({name,email,password}){if(!api())throw new Error('This site has not been connected to Supabase yet.');const {error}=await api().auth.signUp({email,password,options:{data:{full_name:name}}});if(error)throw error;},
 async signIn({email,password}){const {error}=await api().auth.signInWithPassword({email,password});if(error)throw error;await this.sync();},
 async signOut(){if(api())await api().auth.signOut();cache.appliances=[];cache.services=[];cache.family=[];cache.invites=[];cache.currentUser=null;householdId=null;},
 async sync(){if(!api())return;const {data:{user}}=await api().auth.getUser();if(!user){cache.currentUser=null;return;}const [p,m]=await Promise.all([api().from('profiles').select('*').eq('id',user.id).single(),api().from('household_members').select('household_id,role').eq('user_id',user.id).single()]);if(p.error||m.error)throw p.error||m.error;householdId=m.data.household_id;cache.currentUser={...dbToProfile(p.data),role:m.data.role};const [members,invites,a,s]=await Promise.all([api().rpc('get_household_members'),api().rpc('get_household_invites'),api().from('appliances').select('*').order('created_at',{ascending:false}),api().from('services').select('*').order('date',{ascending:false})]);if(members.error||invites.error||a.error||s.error)throw members.error||invites.error||a.error||s.error;cache.family=members.data.map(dbToProfile);cache.invites=invites.data||[];cache.appliances=a.data.map(dbToApp);cache.services=s.data.map(dbToService);},
 getAppliances:()=>cache.appliances,getApplianceById:id=>cache.appliances.find(x=>x.id===id),async saveAppliance(x){const i=cache.appliances.findIndex(a=>a.id===x.id);if(i<0)cache.appliances.unshift(x);else cache.appliances[i]=x;const {error}=await api().from('appliances').upsert(appToDB(x));if(error)throw error;return x;},async deleteAppliance(id){const {error}=await api().from('appliances').delete().eq('id',id);if(error)throw error;cache.appliances=cache.appliances.filter(x=>x.id!==id);cache.services=cache.services.filter(x=>x.applianceId!==id);},
 getServices:()=>cache.services,getServicesForAppliance:id=>cache.services.filter(x=>x.applianceId===id),async saveService(x){const i=cache.services.findIndex(s=>s.id===x.id);if(i<0)cache.services.unshift(x);else cache.services[i]=x;const {error}=await api().from('services').upsert(serviceToDB(x));if(error)throw error;return x;},async deleteService(id){const {error}=await api().from('services').delete().eq('id',id);if(error)throw error;cache.services=cache.services.filter(x=>x.id!==id);},
 getFamilyMembers:()=>cache.family,getPendingInvites:()=>cache.invites,async saveProfile({name,profilePic}){const {error}=await api().from('profiles').update({full_name:name,profile_pic:profilePic||null}).eq('id',cache.currentUser.id);if(error)throw error;await this.sync();},async inviteFamilyMember(name,email){const {error}=await api().rpc('invite_household_member',{invitee_name:name.trim(),invitee_email:email.trim().toLowerCase()});if(error)throw error;await this.sync();},
 getCurrentUser:()=>cache.currentUser,setCurrentUser:x=>(cache.currentUser=x),canModify:()=>Boolean(cache.currentUser),isAdmin:()=>cache.currentUser?.role==='Owner'
};
